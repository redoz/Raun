<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <OutputType>Exe</OutputType>
    <IsPackable>false</IsPackable>
    <IsTestProject>true</IsTestProject>
  </PropertyGroup>

  <!-- Core runtime tests: manifest model, parallel awaiters, the DAG scheduler and the run loop.
       These exercise Raun only and never load a test-platform adapter. The DI implementation is
       here to build a real provider for the per-scenario scope tests. -->
  <ItemGroup>
    <PackageReference Include="Microsoft.Extensions.DependencyInjection" />
    <PackageReference Include="Microsoft.NET.Test.Sdk" />
    <PackageReference Include="xunit.v3.mtp-v2" />
    <PackageReference Include="xunit.runner.visualstudio" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\src\Raun\Raun.csproj" />
  </ItemGroup>

  <!-- Snapshot assertions, shared by the projects that take snapshots. Snapshot files of generated
       C# are data, not sources: they must not be compiled into this project. -->
  <ItemGroup>
    <Compile Include="..\Shared\Snapshot.cs" Link="Snapshot.cs" />
    <Compile Remove="**\*.verified.cs;**\*.received.cs" />
    <None Include="**\*.verified.cs" />
  </ItemGroup>

</Project>
